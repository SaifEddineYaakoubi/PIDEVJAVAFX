# Configuration de la Base de Données
# Editez ce fichier pour adapter les paramètres à votre environnement

# URL de la base de données MySQL
db.url=jdbc:mysql://localhost:3306/smart_farm

# Identifiant MySQL
db.user=root

# Mot de passe MySQL (vide par défaut)
db.password=

# Port MySQL (3306 par défaut)
db.port=3306

# Nom de la base de données
db.name=smart_farm

# Pool de connexions (optionnel)
db.pool.size=10
db.pool.max=20

